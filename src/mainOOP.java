import UI.GeometryUI;

public class mainOOP {
    public static void main(String[] args) {
        geometry geometry = new geometry();
        GeometryUI geo = new GeometryUI();
        geo.mainUI();
        geo.drawAreaOfSquare();


    }
}